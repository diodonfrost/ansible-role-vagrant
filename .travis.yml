---
services:
  - docker

env:
#-----------------------------------------------
#
# CentOS
#
#-----------------------------------------------
  - distribution: centos-8
    playbook: vagrant-latest
  - distribution: centos-8
    playbook: vagrant-2.1.0
  - distribution: centos-7
    playbook: vagrant-latest
  - distribution: centos-7
    playbook: vagrant-2.1.0
  - distribution: centos-6
    playbook: vagrant-latest
  - distribution: centos-6
    playbook: vagrant-2.1.0

#--------------------------------
#
# Fedora
#
#--------------------------------
  - distribution: fedora-32
    playbook: vagrant-latest
  - distribution: fedora-32
    playbook: vagrant-2.1.0
  - distribution: fedora-31
    playbook: vagrant-latest
  - distribution: fedora-31
    playbook: vagrant-2.1.0
  - distribution: fedora-30
    playbook: vagrant-latest
  - distribution: fedora-30
    playbook: vagrant-2.1.0
  - distribution: fedora-29
    playbook: vagrant-latest
  - distribution: fedora-29
    playbook: vagrant-2.1.0
  - distribution: fedora-28
    playbook: vagrant-latest
  - distribution: fedora-28
    playbook: vagrant-2.1.0
  - distribution: fedora-27
    playbook: vagrant-latest
  - distribution: fedora-27
    playbook: vagrant-2.1.0
  - distribution: fedora-26
    playbook: vagrant-latest
  - distribution: fedora-26
    playbook: vagrant-2.1.0

#--------------------------------
#
# Ubuntu
#
#--------------------------------
  - distribution: ubuntu-18.04
    playbook: vagrant-latest
  - distribution: ubuntu-18.04
    playbook: vagrant-2.1.0
  - distribution: ubuntu-16.04
    playbook: vagrant-latest
  - distribution: ubuntu-16.04
    playbook: vagrant-2.1.0
  - distribution: ubuntu-14.04
    playbook: vagrant-latest
  - distribution: ubuntu-14.04
    playbook: vagrant-2.1.0
  - distribution: ubuntu-12.04
    playbook: vagrant-1.8.0

#--------------------------------
#
# Debian
#
#--------------------------------
  - distribution: debian-10
    playbook: vagrant-latest
  - distribution: debian-10
    playbook: vagrant-2.1.0
  - distribution: debian-9
    playbook: vagrant-latest
  - distribution: debian-9
    playbook: vagrant-2.1.0
  - distribution: debian-8
    playbook: vagrant-latest
  - distribution: debian-8
    playbook: vagrant-2.1.0

#--------------------------------
#
# Oraclelinux
#
#--------------------------------
  - distribution: oraclelinux-8
    playbook: vagrant-latest
  - distribution: oraclelinux-8
    playbook: vagrant-2.1.0
  - distribution: oraclelinux-7
    playbook: vagrant-latest
  - distribution: oraclelinux-7
    playbook: vagrant-2.1.0
  - distribution: oraclelinux-6
    playbook: vagrant-latest
  - distribution: oraclelinux-6
    playbook: vagrant-2.1.0

#--------------------------------
#
# Amazonlinux
#
#--------------------------------
  - distribution: amazonlinux-2
    playbook: vagrant-latest
  - distribution: amazonlinux-2
    playbook: vagrant-2.1.0
  - distribution: amazonlinux-1
    playbook: vagrant-latest
  - distribution: amazonlinux-1
    playbook: vagrant-2.1.0

#--------------------------------
#
# Opensuse
#
#--------------------------------
  - distribution: opensuse-15.1
    playbook: vagrant-latest
  - distribution: opensuse-15.1
    playbook: vagrant-2.1.0
  - distribution: opensuse-15
    playbook: vagrant-latest
  - distribution: opensuse-15
    playbook: vagrant-2.1.0
  - distribution: opensuse-42.3
    playbook: vagrant-latest
  - distribution: opensuse-42.3
    playbook: vagrant-2.1.0
  - distribution: opensuse-13.2
    playbook: vagrant-latest
  - distribution: opensuse-13.2
    playbook: vagrant-2.1.0

before_install:
  # Build docker container
  - 'sudo docker pull diodonfrost/${distribution}-ansible:latest'

script:
  - container_id=$(mktemp)

  # Run container and mount ansible role with it
  - 'sudo docker run --detach --privileged -v /sys/fs/cgroup:/sys/fs/cgroup:ro --volume="${PWD}":/etc/ansible/roles/ansible-role-vagrant:ro diodonfrost/${distribution}-ansible:latest > "${container_id}"'

  # Check syntax
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-vagrant/tests/playbooks/${playbook}.yml --syntax-check'

  # Run ansible playbook
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-vagrant/tests/playbooks/${playbook}.yml'

  # Test indempotence
  - >
    sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-vagrant/tests/playbooks/${playbook}.yml
    | grep -q 'changed=0.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)

  # Test role
  - 'inspec exec tests/inspec --chef-license accept-silent -t docker://$(cat ${container_id})'

  # Delete container
  - 'sudo docker rm -f "$(cat ${container_id})"'

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
